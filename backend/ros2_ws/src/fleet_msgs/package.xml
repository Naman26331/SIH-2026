<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://relaxng.org/ns/structure/1.0"?>
<package format="3">
  <name>fleet_msgs</name>
  <version>0.1.0</version>
  <description>
    The things FLEET-X robots say to each other.

    One message type per message class in shared/fleetx_core/messages.py, with
    the same field names, so the translation either side is a straight copy and
    there is nowhere for the two to drift apart.
  </description>
  <maintainer email="fleetx@example.com">FLEET-X team</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <depend>builtin_interfaces</depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
